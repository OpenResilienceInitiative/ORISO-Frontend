# 03 — Progress log

| #   | Status    | Target                                                                                                                                                                                                                                                                                                   | Change                                     | Verify                                                                                                                                       |
| --- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | pass      | Intake: issue + annotated image transcribed, branch from dev @ 501959f3, lane → In progress                                                                                                                                                                                                              | task folder                                | —                                                                                                                                            |
| 2   | pass      | Phase 1 root cause (verified in code, not assumed): (a) `onNewMessageEvent` drops a `roomId` absent from the loaded list — `touchSessionsByRids` returns `null` for unknown rooms → new user never appears; (b) `anonymousEnquiryAccepted` raised a toast only, no refresh emission → request→chat stale | —                                          | reads of `SessionsList.tsx:808-935`, `WebsocketHandler.tsx:95-245`                                                                           |
| 3   | red→green | TDD helpers: `app/stompListRefresh.ts` (+test) and `sessionsList/liveListRefresh.ts` (+test)                                                                                                                                                                                                             | 2 new modules, 2 new test files            | RED: "Cannot find module" ×2 → GREEN 11/11                                                                                                   |
| 4   | pass      | Wire both break points: SessionsList refetches on an unknown room (throttled); WebsocketHandler emits both refresh flags on `anonymousEnquiryAccepted` and keeps the toast                                                                                                                               | `SessionsList.tsx`, `WebsocketHandler.tsx` | `npx vitest run --project unit src/components/sessionsList src/components/app/stompListRefresh.test.ts` → 20 files / 125 tests; eslint clean |
