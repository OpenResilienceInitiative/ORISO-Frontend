# 04 — Test evidence (#1200)

Node 22.12.0, `ORISO-Frontend` on `claude/1200/timeline-view-clarifications`.

- TDD RED: `npx vitest run --project unit src/components/notificationsCenter/timelineFilter.test.ts src/components/notificationsCenter/MarkAllReadButton.test.tsx` → 3 failures (`hasUnreadTimelineItems is not a function`) + unresolved import `./MarkAllReadButton` (expected)
- TDD GREEN: same command → pass; then `npx vitest run --project unit src/components/notificationsCenter` → 10 files / 84 tests pass
- `npx eslint --max-warnings=0 src/components/notificationsCenter` → pass; `npx stylelint src/components/sessionsList/sessionsList.styles.scss` → pass
- `npm run lint:scripts` (eslint + tsc) → pass; `npm run test:unit` → **368/368 files, 4027/4027 tests pass**
- **Live check on dev.oriso.org** (dev server :9001 serving this branch, counsellor logged in by the user, Browser pane, mobile layout): Zeitstrahl shows the data-driven chip row **Anfragen · Nachrichten · System · Ungelesen · ✓✓** for a feed with 131 cards (requests, messages, waiting-room/system events) — i.e. the "extended filter bar" renders every family present (JOB3). DOM read via the pane: `button[aria-label="Alle als gelesen markieren"]` → `disabled: false` while unread cards exist (JOB1 fix active). With the account owner's consent the button was pressed: afterwards `disabled: true`, computed `opacity: 0.38`, 0 unread cards (120 cards loaded) — reviewer test plan step 1 passes; chat badges unaffected. Card click on the mobile layout follows the action target (Requests view) — the desktop detail pane/preview (JOB2) is covered by `ConversationPreview` code comments + `ConversationPreview.test.tsx`.
- Pre-existing defect noticed (out of scope, follow-up): message-event previews render raw alignment markup `[[align:left]]<p>are you there</p>[[/align]]` in the card text.
- Pre-existing local-dev noise: webpack overlay "Unexpected token '<'" from the SockJS live-service fallback through the dev proxy (see #1193 notes); unrelated.
- Review round 1 (CodeRabbit): `hasUnreadTimelineItems(notificationFeed)` replaced by the provider's server-backed `unreadNotificationCount > 0` (feed is paginated; `unreadCount` comes from the server with every feed refresh, adjusted live on read / new event). Helper + its 3 tests removed; `MarkAllReadButton.test.tsx` unchanged. Targeted run: 10 files / 81 tests pass; full gate: `npm run lint:scripts` pass, `npm run test:unit` 368/368 files, 4024/4024 tests
