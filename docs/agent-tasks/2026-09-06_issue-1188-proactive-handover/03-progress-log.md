# 03 — Progress log

| #   | Status     | Target                                                                                                                                                                                                        | Change                                                                                                                                                                                  | Verify                                                                                           |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 1   | pass       | Intake: issue + annotated image transcribed, branch from dev, lane → In progress, sync #1178 confirmed merged (05175d2a in dev)                                                                               | task folder                                                                                                                                                                             | `git branch -a --contains 05175d2a` → upstream/dev                                               |
| 2   | pass       | Job 1 investigated: own pickaxe → "Fall Übergabe" absent from all refs; menu has no handover entry; profile link resolves even with an encoded Matrix room id                                                 | `SessionsZone.routing.test.tsx` +4 guard cases                                                                                                                                          | `npx vitest run --project unit src/components/app/SessionsZone.routing.test.tsx` → 15 pass       |
| 3   | pass       | Job 2 verified on dev (no change needed): `05175d2a` is an ancestor of `upstream/dev`; handover notice renders as a Carimat system message                                                                    | —                                                                                                                                                                                       | Storybook shots desktop + 320px (`screenshots/0{1,2,3}-job2-*.png`), no "needs live data" panel  |
| 4   | red→green  | Job 3 fix (TDD): `AskerInfo` renders `UserAvatar` (72px) keyed on `askerMatrixUserId ?? username`; dead `&__icon--user` CSS + white plate removed                                                             | `askerInfo/AskerInfo.tsx(.test.tsx)`, `askerInfo.styles.scss`                                                                                                                           | 4 new cases failed first ("Unable to find user-avatar"), then 4/4 pass; eslint + stylelint clean |
| 5   | fail→fixed | PR showed `CONFLICTING` so no CI ran; `dev` had moved 11 commits                                                                                                                                              | merged `upstream/dev`; `.gitignore` conflict → keep both task-folder whitelists                                                                                                         | `git merge` clean afterwards                                                                     |
| 6   | changed    | **Job 3 superseded on dev by PR #1307 (issue #1192)** — it rebuilt AskerInfo with `AnimalAvatar` + responsive size + ARIA while this PR was open                                                              | took dev's `AskerInfo.tsx` and `askerInfo.styles.scss` wholesale; rewrote `AskerInfo.test.tsx` against dev's implementation                                                             | `npx vitest run --project unit src/components/askerInfo/AskerInfo.test.tsx` → 5/5                |
| 7   | pass       | CI unblocked once the merge was pushed: both "Frontend PR Validation" jobs green                                                                                                                              | —                                                                                                                                                                                       | run 34023157599 success                                                                          |
| 8   | fail→fixed | CodeRabbit: the "enquiry without a Matrix room" case used the `sessionView` path, but `SessionMenu` builds enquiry links from the ENQUIRY list path (`sessionPreview`) — so it never exercised the real route | fixture gained the two real `sessionPreview` `userProfile` routes (+ the preview detail route); the enquiry case now generates from `sessionPreview`, plus a new with-room enquiry case | 16/16; proven to guard: removing the two fixture routes fails exactly those 2 cases              |
